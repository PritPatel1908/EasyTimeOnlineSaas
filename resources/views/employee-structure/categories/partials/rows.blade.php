@forelse($categories as $category)
<tr><td><input id="category_{{ $category->id }}" class="form-check-input" type="checkbox"></td><td><h6 class="fw-medium fs-14 mb-0">{{ $category->name }}</h6></td><td>{{ $category->code }}</td><td>{{ $category->email ?? '—' }}</td><td>{{ $category->companies->pluck('name')->join(', ') ?: '—' }}</td><td>{{ $category->locations->pluck('name')->join(', ') ?: '—' }}</td><td><span class="badge {{ $category->status === 1 ? 'badge-success' : 'badge-danger' }}">{{ $category->status === 1 ? 'Active' : 'Inactive' }}</span></td><td class="text-center"><div class="action-icon d-inline-flex">@if(\App\Support\TenantPermissions::userCan('Category','read'))<a class="me-2" href="{{ url('employee-structure/categories/'.$category->id) }}" title="View"><i class="ti ti-eye"></i></a>@endif @if(\App\Support\TenantPermissions::userCan('Category','write'))<a class="me-2" href="{{ url('employee-structure/categories/'.$category->id.'/edit') }}" title="Edit"><i class="ti ti-edit"></i></a>@endif @if(\App\Support\TenantPermissions::userCan('Category','delete'))<a href="javascript:void(0)" class="text-danger delete-category-btn" data-url="{{ url('employee-structure/categories/'.$category->id) }}" data-name="{{ $category->name }}" data-bs-toggle="modal" data-bs-target="#delete_category_modal" title="Delete"><i class="ti ti-trash"></i></a>@endif</div></td></tr>
@empty
<tr><td colspan="8" class="py-4 text-center text-muted">No categories found.</td></tr>
@endforelse
